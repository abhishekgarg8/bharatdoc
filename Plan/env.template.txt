# ============================================================
# BharatDoc — Environment Variables Template
# ============================================================
# Fill in all values below, then split into two files:
#   - .env.local  → Vercel (Next.js frontend)
#   - .env        → Railway (backend worker)
# ============================================================


# ============================================================
# FILE 1: .env.local  (Vercel / Next.js Frontend)
# ============================================================

# --- Supabase (public anon key — browser safe) ---
# Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=

# Supabase Dashboard → Settings → API → anon / public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Supabase (server-only Vercel runtime secrets — NEVER prefix with NEXT_PUBLIC) ---
# Supabase Dashboard → Settings → API → Project URL (same as public URL above)
SUPABASE_URL=

# Supabase Dashboard → Settings → API → service_role secret key
SUPABASE_SERVICE_ROLE_KEY=

# Public app URL used by Supabase confirmation and magic links.
# Production: https://bharatdoc-web.vercel.app/
NEXT_PUBLIC_SITE_URL=

# Demo mode should stay false/missing in production. Set true only for local demos/test previews.
NEXT_PUBLIC_ENABLE_DEMO_MODE=false

# --- Railway Worker ---
# Railway Dashboard → your worker service → Settings → Public URL
# Example: https://bharatdoc-worker.railway.app
# Public value used by the browser to upload audio directly to Railway.
NEXT_PUBLIC_RAILWAY_WORKER_URL=

# Server-side value used by Vercel API routes for summary/PDF worker calls.
RAILWAY_WORKER_URL=


# ============================================================
# FILE 2: .env  (Railway Backend Worker — NEVER expose publicly)
# ============================================================

# --- OpenAI ---
# platform.openai.com → API Keys → Create new secret key
# Used for: gpt-4o-mini-transcribe (audio) + gpt-4o-mini (summaries)
OPENAI_API_KEY=

# --- Supabase (elevated service role — server only) ---
# Supabase Dashboard → Settings → API → Project URL (same as above)
SUPABASE_URL=

# Supabase Dashboard → Settings → API → service_role secret key
SUPABASE_SERVICE_ROLE_KEY=

# Comma-separated browser origins allowed to call the worker directly.
# Example: https://bharatdoc-web.vercel.app,http://localhost:3000,http://127.0.0.1:3000
WORKER_CORS_ORIGINS=


# ============================================================
# DEPLOYMENT CHECKLIST
# ============================================================
# Vercel must have:
#   NEXT_PUBLIC_SUPABASE_URL
#   NEXT_PUBLIC_SUPABASE_ANON_KEY
#   NEXT_PUBLIC_SITE_URL
#   NEXT_PUBLIC_ENABLE_DEMO_MODE=false
#   NEXT_PUBLIC_RAILWAY_WORKER_URL
#   RAILWAY_WORKER_URL
#   SUPABASE_URL
#   SUPABASE_SERVICE_ROLE_KEY
#
# Railway must have:
#   OPENAI_API_KEY
#   SUPABASE_URL
#   SUPABASE_SERVICE_ROLE_KEY
#   WORKER_CORS_ORIGINS
#   OPENAI_TRANSCRIPTION_MODEL (optional override)
#   OPENAI_SUMMARY_MODEL (optional override)
#
# Supabase Auth must allow redirect URL:
#   https://bharatdoc-web.vercel.app/
